# Alpha Vantage API Key (optional - get free key at https://www.alphavantage.co/support/#api-key)
# If not set, the app will use yfinance instead
ALPHA_VANTAGE_API_KEY=demo

# API Mode: 'alphavantage' or 'yfinance'
# yfinance = free, no API key needed (default)
# alphavantage = premium data but requires API key
API_MODE=yfinance
